filepaths:
  repo: /home/spn1560/enz-rxn-data
  data: ${.repo}/data
  artifacts: ${.repo}/artifacts
  raw_data: ${.data}/raw
  interim_data: ${.data}/interim
  processed_data: ${.data}/processed
  raw_mcsa: ${.raw_data}/mcsa
  mcsa_mols: ${.raw_mcsa}/mols
  mcsa_mech_steps: ${.raw_mcsa}/mech_steps
  rules: ${.artifacts}/rules
uniprot_fn: uniprotkb_reviewed_true_2025_04_01.tsv
uniprot: ${filepaths.raw_data}/pathway/${uniprot_fn}
rhea_smiles: ${filepaths.raw_data}/pathway/rhea-reaction-smiles.tsv
rhea_directions: ${filepaths.raw_data}/pathway/rhea-directions.tsv
chebi2name: ${filepaths.raw_data}/pathway/chebiId_name.tsv
chebi2smiles: ${filepaths.raw_data}/pathway/rhea-chebi-smiles.tsv
rm_stereo: true
